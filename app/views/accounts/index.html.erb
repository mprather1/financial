<h1>Accounts</h1>
<table>
  <tr>
    <th>Description</th>
    <th>Total Balance</th>
  </tr>
  <% @accounts.each do |f| %>
    <tr>
      <td><%= link_to f.description, account_path(f) %></td>
      <td><%= f.balance %></td>
    </tr>
  <% end %>
</table>
